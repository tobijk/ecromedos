#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:

override_dh_auto_test:

override_dh_auto_install:
	install -d debian/ecromedos/usr/share/ecromedos
	cp -a bin etc lib xslt style data debian/ecromedos/usr/share/ecromedos/
